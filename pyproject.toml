[project]
name = "photo-watermark"
version = "0.1.0"
description = "Batch add text or logo watermarks to photography images."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "Pillow>=10.0.0",
]

[project.scripts]
photo-watermark = "watermark:main"

[tool.ruff]
line-length = 100
